# Age Detection Model

Detects a person's age from an image using deep learning / computer vision.

## Requirements
```bash
pip install -r requirements.txt
```

## Usage
```bash
python age_detection.py
```

## Files
- `age_detection.py` — main script
- `requirements.txt` — dependencies
